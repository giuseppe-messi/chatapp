# ChatApp

A full-stack real-time chat application built with **React**, **TypeScript**, and **Node.js**.

## 🚀 Overview

**ChatApp** is a modern real-time chat platform designed to demonstrate clean architecture, full-stack TypeScript practices, and production-ready setup for messaging features.

- Real-time messaging with **Socket.IO**
- Secure authentication with sessions and hashed passwords
- Type-safe backend with **Prisma ORM**
- Optimistic UI updates using **React Query**
- Responsive and modern UI powered by **Tailwind CSS**
- PostgreSQL for relational data, MongoDB for chat messages
- Clean separation between client and server

## 🧩 Tech Stack

### **Frontend**

- React + TypeScript
- React Query (data fetching & caching)
- Tailwind CSS
- Axios (HTTP client)
- React Router

### **Backend**

- Node.js + Express
- Prisma ORM (PostgreSQL)
- MongoDB (chat storage)
- Socket.io (real-time communication)
- Cookie-based session auth

---

## 📂 Project Structure

```bash
chatapp/
├── client/ # React front-end
├── server/ # Node.js + Express + Prisma backend
```

## ⚡Check it live!

#### Heads up: The app runs on free-tier hosting (Render & Netlify). It may take a few seconds to wake up after inactivity, so if it seems stuck, try refreshing the page a couple of times, everything works normally once active.

#### live link: [https://chat-appi.netlify.app](https://chat-appi.netlify.app)
